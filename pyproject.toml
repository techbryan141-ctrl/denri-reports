[project]
name = "denri-reports"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask==3.1.2",
    "gspread==6.2.1",
    "google-auth==2.41.1",
    "pandas==2.3.3",
    "python-dotenv==1.2.2",
]

[tool.vercel]
entrypoint = "run:app"
